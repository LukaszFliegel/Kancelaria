@model AjaxContractorEditModel<KompensataPowiazanie>

<div id="kanc_pozycja_kompensaty_dialog" class="kanc-ajax-dialog-submit" data-dialog-width="900" data-dialog-height="360" title="Dodaj pozycję kompensaty">
    @using (Ajax.BeginForm(Model.Action, Model.Controller,
        new AjaxOptions { HttpMethod = "Post", UpdateTargetId = Model.GridElementId, InsertionMode = Model.InsertionMode }
        )
    )
    {
        @Html.AntiForgeryToken()
        @Html.ValidationSummary(true)

        <fieldset>
            <legend>Pozycja kompensaty</legend>

            @Html.Hidden("Id", Model.Model.Id)
            @Html.Hidden("DialogElementId", Model.DialogElementId)
            @Html.Hidden("GridElementId", Model.GridElementId)
            <div class="kanc-fieldset-left">
                @*<div class="editor-field">
                    @Html.Lookup("IdFakturySprzedazy", value: Model.Model.IdFakturySprzedazy, controller: "FakturySprzedazy", multiselect: false, chooseText: "Wybierz", 
                    cancelText: "Anuluj", title: "Wybierz fakturę sprzedaży", paging: true)
                </div>*@
                <div class="editor-label">
                    @Html.Label("Wybierz fakturę sprzedaży")
                </div>
                <div class="editor-field">
                    @Html.Lookup("IdFakturySprzedazy", value: Model.Model.IdFakturySprzedazy, controller: "FakturySprzedazy", multiselect: false, chooseText: "Wybierz", 
                    cancelText: "Anuluj", title: "Wybierz fakturę sprzedaży", paging: true, parameters: new Dictionary<string, object>() { { "idKontrahenta", Model.IdKontrahenta } }, data: new Dictionary<string, string>() { { "JSMethod", "GetJSON" } })
                </div>
            </div>

            <div class="kanc-fieldset-right">
                <div class="editor-label">
                    @Html.Label("Wybierz fakturę zakupu")
                </div>
                <div class="editor-field">
                    @Html.Lookup("IdFakturyZakupu", value: Model.Model.IdFakturyZakupu, controller: "FakturyZakupu", multiselect: false, chooseText: "Wybierz", 
                    cancelText: "Anuluj", title: "Wybierz fakturę zakupu", paging: true, parameters: new Dictionary<string, object>() { { "idKontrahenta", Model.IdKontrahenta } }, data: new Dictionary<string, string>() { { "JSMethod", "GetJSON" } })
                </div>

                <div class="editor-label">
                    @Html.Label("Kwota")
                </div>
                <div class="editor-field">
                    @Html.TextBox("Kwota", Model.Model.Kwota)
                </div>
            </div>
            @*<div class="editor-label">
                @Html.Label("Cena jednostkowa")
            </div>
            <div class="editor-field">
                @Html.TextBox("CenaJednostkowa", Model.Model.CenaJednostkowa)
            </div>
            <div class="editor-label">
                @Html.Label("Ilość jednostkowa")
            </div>
            <div class="editor-field">
                @Html.TextBox("Ilosc", Model.Model.Ilosc)
            </div>
            <div class="editor-label">
                @Html.Label("Stawka Vat (%)")
            </div>
            <div class="editor-field">
                @Html.TextBox("StawkaVat", Model.Model.StawkaVat)
            </div>
            <div class="editor-label">
                @Html.Label("Inwestycja")
            </div>
            <div class="editor-field">
                @Html.Lookup("IdInwestycji", value: Model.Model.IdInwestycji, controller: "Inwestycje", multiselect: false, chooseText: "Wybierz", 
                cancelText: "Anuluj", title: "Wybierz inwestycję", paging: true)
            </div>

            <div class="editor-label">
                @Html.Label("Opis")
            </div>
            <div class="editor-field">
                @Html.TextBox("Opis", Model.Model.Opis)
            </div>*@
        </fieldset>
    }
</div>
